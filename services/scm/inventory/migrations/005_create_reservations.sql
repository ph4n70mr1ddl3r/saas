CREATE TABLE reservations (id TEXT PRIMARY KEY, item_id TEXT NOT NULL REFERENCES items(id), warehouse_id TEXT NOT NULL REFERENCES warehouses(id), quantity INTEGER NOT NULL, reference_type TEXT NOT NULL, reference_id TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active' CHECK(status IN ('active','fulfilled','cancelled')), created_at TEXT NOT NULL DEFAULT (datetime('now')), fulfilled_at TEXT);
